name: ggomdol_pedometer_example
description: Demonstrates how to use the pedometer plugin.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  ggomdol_pedometer:
    path: ../
  flutter:
    sdk: flutter

  permission_handler: ^11.3.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
